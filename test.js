// bds: this file should not be removed from the git repo, since it's not being used

// const expect = require('chai').expect
// const { exec } = require('child_process');
//
//   describe('usage message for invalid commands', () => {
//        it('displays usage message for empty command', () => {
//           exec('./tasks', (err, stdout, stderr) => {
//             expect(stdout).to.include('USAGE')
//           });
//        });
//        it('displays usage message for invalid command', () => {
//           exec('./tasks add', (err, stdout, stderr) => {
//             expect(stdout).to.include('USAGE')
//           });
//         });
//   });
